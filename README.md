# Zenn content

このレポジトリは、親レポジトリ`gumigumih/articles`からサブモジュールとして利用するZenn用コンテンツです。

- 記事: `articles/`
- 本: `books/`
- 画像: `images/`

```bash
npm install
npm run preview
npm run new -- --slug <記事のスラッグ>
```
